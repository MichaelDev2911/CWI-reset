
package principaligualdadenomes;

/**
 *
 * @author michael
 */
public class Nomes {
    /* mesmo recebendo strings como argumento
     * a função ou metodo deve ser do
     * tipo do retorno  desejado. */
    public boolean verificar(String nomeFulano,String nomeBeltrano){
       boolean igualdade = (nomeFulano.equals(nomeBeltrano));
        return igualdade;
    }
}
